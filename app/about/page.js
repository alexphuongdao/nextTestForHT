export default function AboutPage(){
    return (
    <main>
        <h1>About me</h1>
    </main>
    );
}

// this is the page that help render the about 
// nextJS system by folder system by itself is the 
// structure of the actual route as well